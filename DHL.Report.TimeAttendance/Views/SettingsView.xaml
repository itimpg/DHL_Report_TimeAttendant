<Controls:MetroWindow   
        x:Class="DHL.Report.TimeAttendance.Views.SettingsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DHL.Report.TimeAttendance.Views"
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        mc:Ignorable="d"
        BorderBrush="{DynamicResource AccentColorBrush}"
        BorderThickness="1"
        WindowStartupLocation="CenterScreen"
        EnableDWMDropShadow="True"
        Title="Settings" Height="300" Width="300"
        DataContext="{Binding Path=Setting, Source={StaticResource Locator}}">
    <Grid Margin="10">

        <StackPanel>
            <TextBox Controls:TextBoxHelper.Watermark="This is a textbox" />
            <Button Style="{DynamicResource SquareButtonStyle}"
                Command="{Binding SaveCommand}"
                CommandParameter="{Binding Config}"
                Content="Save"/>
        </StackPanel>
        
        <Grid Background="White" Opacity="0.8" Visibility="{Binding IsLoading, Converter={StaticResource VisibleConverter}}">
            <Controls:ProgressRing Foreground="{DynamicResource AccentColorBrush}"/>
        </Grid>
    </Grid>
</Controls:MetroWindow>
